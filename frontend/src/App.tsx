import React from 'react';

import OverviewPage from './containers/OverviewPage';

const App = () => {
  let content = (
    <React.Fragment>
      <OverviewPage />
    </React.Fragment>
  );

  return content;
};

export default App;
